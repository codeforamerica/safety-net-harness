form:
  id: application-scrollable-ca-applicant
  title: Scrollable (CA - Applicant)
  schema: Application
  layout:
    navigation: none
    display: scrollable
  role: applicant
  pages:
    - id: programs
      title: Programs
      fields:
        - ref: programsAppliedFor
          component: checkbox-group
          labels:
            SNAP: CalFresh (Food Assistance)
            Medicaid_MAGI: Medi-Cal (MAGI)
            TANF: CalWORKs (Cash Assistance)
        - ref: isExpedited
          component: radio
          labels:
            "true": "Yes"
            "false": "No"

    - id: personal-info
      title: Personal Information
      fields:
        - ref: household.members.0.firstName
          component: text-input
          width: half
        - ref: household.members.0.lastName
          component: text-input
          width: half
        - ref: household.members.0.dateOfBirth
          component: date-input

    - id: household-info
      title: Household
      fields:
        - ref: household.size
          component: text-input
        - ref: household.livingArrangement
          component: select
        - ref: household.physicalAddress.street1
          component: text-input
        - ref: household.physicalAddress.city
          component: text-input
          width: half
        - ref: household.physicalAddress.state
          component: text-input
          width: half

    - id: review-sign
      title: Review & Sign
      fields:
        - ref: consentToVerifyInformation
          component: radio
          labels:
            "true": "Yes"
            "false": "No"
        - ref: signatureOfApplicant
          component: text-input
